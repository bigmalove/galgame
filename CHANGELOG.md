# 更新日志 (Changelog)

所有版本的更新内容都将记录在此文件中。

---

## [Unreleased]

### ✨ 新增功能

**地图系统重做：剧情驱动的场景线稿地图**

- 地图不再依赖「世界地图点/地图元素表」和手动上传底图，改为 **AI 根据剧情自动绘制当前详细地点的平面线稿**：LLM（`generateRaw` 独立模式）输出结构化布局 JSON（墙体/建筑/门/道路/水体/植被/区域/家具/地标 9 类元素 + 人物站位锚点），前端用 rough.js 渲染成手绘风格 SVG，渲染 100% 确定不出错（固定种子，同布局每次渲染一致）。
- **5 种线稿风格**可在地图内下拉切换（素笺墨线 / 夜幕墨稿 / 蓝图制图 / 古卷舆图 / 淡彩极简），默认「自动」跟随皮肤深浅；切换零成本仅重渲染（设置项 `mapSceneStyle`）。
- **布局持久缓存**：按「角色卡 + 地点名」存 localStorage（单角色上限 40 地点 LRU 淘汰），重访地点直接命中缓存不再调 AI；顶栏「重新生成」按钮确认后重绘。AI 输出坏 JSON 时自动修复→重试→程序化兜底布局（兜底不落缓存，顶栏黄条提示），任何情况下地图都能打开。
- **在场角色上图**：读「重要角色表」筛选（所在地点匹配 + 在场状态），主角与 NPC 以「缩小立绘 + 名字」标记分布在布局锚点上（无立绘按性别回退路人剪影，再兜底首字头像）；主角金框 ☆ 标识。
- **点击 NPC 弹交互窗**：展示立绘与档案（介绍/外貌/穿着/关系/过往经历折叠），三个动作——对话（「交互选项」列点选 + 自由输入兜底）、送礼（读「背包物品表」）、使用技能（读「主角技能表」），动作以第三人称文本自动发送到聊天并级联关闭弹窗。
- **点击主角弹状态窗**：4 个 Tab 分别展示「主角信息表 / 主角技能表 / 背包物品表 / 任务与事件表」。
- 沿用并抽取旧地图的手势引擎（滚轮缩放/拖拽平移/双指捏合/边界钳制，拖动后吞误触点击）；弹窗全皮肤同步 + 移动端全屏适配。入口不变：点击顶部地点栏（或资源管理器地图页）。
- 旧地图的底图存储、坐标设置与资源包导入导出字段全部保留，不影响既有资源包兼容性。

**地图布局持久化到数据库「地图数据表」**

- 生成的布局 JSON 自动写入数据库插件（SP·数据库 V）的一张独立表「地图数据表」（列：地点/布局JSON/更新时间），数据随聊天消息保存——导出聊天、开分支、换设备都不丢，同地点不再重复生成。
- 首次写入自动把表注册进**聊天指导表**（`importTemplateFromData scope:'chat'`，不污染全局模板与其他聊天）；表配置 `updateFrequency=0` + `exportConfig.enabled=false`，**不进填表 AI 提示词、不进世界书**，零 token 开销；表格 Note 声明禁止 AI 修改。
- 读取优先级：数据库表 → localStorage 本地缓存（双写）。数据库插件版本过旧（无写入 API）或写入与 AI 填表撞车被拒时，自动退回本地缓存，功能不受影响。「重新生成」会同时清除两层数据。

**地图由填表 AI 直接生成（零额外 LLM 调用）**

- 新增「地图生成表」（工作台）：参与每轮 AI 填表，表格 Note 内置完整的布局生成规范（画布/元素类型/布局要求/紧凑 JSON 约束 + SQL 示例）。填表 AI 发现「当前详细地点」还没有地图时，**在填表的同一次输出里顺便 INSERT 一行布局 JSON**——不需要任何额外的 AI 调用，地点名与全局数据表天然一致。
- **收割机制**：填表完成回调（3 秒防抖）触发收割——把工作台里的布局 JSON 清洗校验后搬进「地图数据表」永久保存（跟随聊天）+ 写本地缓存，工作台行只留「（已存档）」占位。因此填表提示词里的地图开销恒定极小（规范 + 每地点一行占位），不随地图数量膨胀。
- 已存档/无效占位行同时兼作防重标记（AI 规则：地点已有行就不再生成）；解析失败的行标记「（无效，已丢弃）」，该地点打开地图时走即时生成兜底。收割写回与下轮填表撞车被拒时，下次回调自动重试（幂等）。
- 双表随脚本启动自动注册进**当前聊天的指导表**（每个聊天独立；插件晚加载时 5 秒 × 12 次重试）。首轮填表时若尚未注册，从第二轮起生效。填表 AI 未生成时，打开地图仍会即时生成（原逻辑不变）。
- **地点显示兜底**：全局数据表的「当前详细地点」为空/漏填时，顶栏地点显示与地图场景读取自动回退「地图生成表」最新生成的地点（工作台行按生成顺序追加，最后一个非空地点即当前），不再显示「未知地点」。
- **地点列名兼容**：全局数据表的地点列新增「主角当前所在地点」等别名，并加模糊兜底（任何包含「地点/位置」且不含「时间」的列都能识别），兼容 Storyteller 等不同表格模板的命名。
- **主角状态窗支持使用技能/道具**：「技能」「背包」Tab 的每行末尾新增「使用」按钮，点击填入「{主角}使用了「XX」。」/「{主角}从背包中取出「XX」，使用了它。」并级联关闭状态窗与地图。名称列按别名定位（技能名称/物品名称等），定位失败时退化为纯表格不影响展示。
- **地图动作改为填入输入框（不自动发送）**：所有地图动作（NPC 对话/送礼/使用技能、主角使用技能/道具）点击后只把动作文本写进聊天输入框并聚焦，玩家可继续补充描写后自行发送。
- **适配 SQL_v4.3 表格模板**：重要角色表新增「身份/基础属性/特有属性」列的读取与展示（NPC 交互窗信息区）；该模板无技能表——技能面板与状态窗技能 Tab 自动回退解析「主角信息·特有属性」列（`爆裂魔法:85; 时间回溯:70` 格式，显示为成功率）；物品表的「品质」列并入类别展示；状态窗新增「装备」Tab（仅在装备表存在时显示）；全局数据表/任务表列名差异自动兼容。
- **在场判定泛化**（修复部分模板全员不上地图）：支持反义的「是否离场」列（值「否」/空 = 在场，「是/已离场」= 离场）；重要角色表没有「所在地点」列或未填地点时视为跟随当前场景，不再因地点缺失被过滤；「性别/年龄」合并列自动拆分（立绘性别回退可用）；NPC 交互窗新增「持有的重要物品」展示。
- **即时生成与填表生成防重打通**：手动打开地图触发的即时生成入库时，同步在「地图生成表」登记「（已存档）」占位行，避免填表 AI 下一轮重复生成同一地点并覆盖已有地图。

### 🔧 优化

**内置背景图包改为一键导入到当前图包**

- 「内置背景图包」浏览弹窗的「一键导入」不再新建以内置包名命名的独立图包，改为直接导入到**当前启用的图包**；导入完成后不再弹「是否切换图包」确认，真正一键完成（仅存在同名场景需覆盖时保留安全确认）。
- 图包切换下拉里的内置包入口行为不变：仍为导入到以内置包名命名的独立图包并切换为当前图包。

### 🐛 修复

**路人剪影不按性别分配（路人男/路人女永远选不中）**

- 角色被 TTS 自动绑定具体音色后，解析器把 `[表情,男声/女声]` 里的性别标签替换成具体音色名，立绘层拿不到性别信息，剪影回退永远落在中性「路人」。现在性别标签经 `tts="voiceTag=男声/女声"` 独立透传，并按说话人做会话级缓存（后续省略标签的对白同样命中）。
- 加强模式的格式化文本此前在简化格式预处理**之后**才替换原文，`角色[表情,男声]: "对话"` 漏掉预处理，标签泄漏进说话人名（既查不到立绘也没有性别信息）。现在先替换格式化文本再统一预处理。
- 纯文本解析路径（无 `<p>` 包裹或漏预处理的行）新增说话人内联标签剥离：还原真实角色名并回收表情与性别信息。
- 剪影性别推断新增兜底：无标签时依次查会话性别缓存、已绑定音色所属的默认男声/女声音色池。
- 角色重挂载（翻页/刷新恢复）同样走剪影回退，不再退化为占位符。

**COT 条目被禁用后永远不再注入**

- 世界书条目 `Galgame输出格式规范` 一旦被手动/意外禁用，插件更新条目时只写 content 不恢复 enabled，导致 COT 内容存在却永远不进提示词。现在每次更新条目时强制恢复 `enabled: true`。

**生成中断后脚本世界书永久挂载且恢复逻辑失效**

- 恢复逻辑此前只挂在 `GENERATION_ENDED` 上：手动停止/生成出错时只触发 `GENERATION_STOPPED`，世界书残留挂载；下一次生成检测到"已存在"又会把 `isInjected` 置 false，恢复从此永久失效。现在 `GENERATION_STOPPED` 同样触发恢复，且检测到中断残留时保留原始列表、本次生成结束后照常恢复。

**未生成时误显示「生成中」图标（后台调用/干跑被误认为对话生成）**

- 两个 `GENERATION_STARTED` 监听器此前忽略事件参数 `(type, option, dry_run)`，把酒馆所有 `Generate()` 调用一律当成对话生成：切角色/切预设时的 token 计算干跑（`dry_run=true`）、其他插件的后台静默生成（总结/数据库等走 `type='quiet'`）都会点亮「生成中」图标；且这类调用没有配对的 `GENERATION_ENDED`，图标要挂到 120 秒超时保护才消失。现在统一跳过 `dry_run` 与 `quiet`（`quietToLoud` 除外）。
- 世界书按需附加监听器同样受此影响：干跑也会临时挂载/摘除脚本世界书且等不到恢复事件，现已一并过滤。

### ✨ 新增功能

**配置向导（首次使用引导）**

- 新增七步配置向导：欢迎 → 显示模式选择（标准 Galgame / 简化图书绘本，自动开启主开关）→ 皮肤选择（实时预览）→ 常用设置（Pixi 特效 / 情境样式 / 启用 BGM / 背景图填充，统一应用并合并一次 COT 注入）→ AI 生图配置（来源选择，推荐智绘姬）→ 图包导入（内置图包 / 本地压缩包 / 远程压缩包 / 远程链接 JSON）→ 完成刷新视图。
- 手动入口：设置 → 通用 →「刷新视图」下方的「配置向导」按钮。
- 自动弹出：检测到全库无任何资源（立绘/背景/地图/CG 均为 0）时自动弹出，带「不再自动提示」勾选（localStorage 持久化），每次会话最多弹一次，标题画面显示中不打扰。
- 调试入口：`galgame.showSetupWizard()`。

**「生成剧情CG」升级为生图通用开关**

- 原「大香蕉 CG模式」「Wallhaven CG模式」两个分散开关合并为生图配置顶部的通用「生成剧情CG」开关（`imageGenCgMode`，旧设置任一开启自动迁移）：开启生成包含人物的剧情CG，关闭生成纯场景背景。
- 覆盖范围扩展到全部四个内置来源：ComfyUI 不再强制追加 `no humans`；NovelAI 在 CG 模式下自动从提示词前后缀剔除 `no humans / scenery / background`；大香蕉、Wallhaven 沿用原逻辑；COT 注入的生图规范同步区分 CG / 纯场景模式。
- 智绘姬（st-chatu8）由其插件自行生成图片，不受此开关影响，选中智绘姬时开关自动隐藏。

**简化图书绘本模式支持背景图包**

- 绘本模式的 COT 现在也注入本地图库（含内置背景图包）的场景名列表，模型可输出 `<background scene="…" />` 切换图包背景。
- 背景来源设为「关闭」时，只要图库中有背景，绘本模式仍会注入图库背景规范（图库为空时照旧不注入）。

## [0.9] - 2026-07-04

### ✨ 新增功能

**都市包新增 10 套 NPC 配角模板（图包 city v1.4.0）**

- 现代都市包扩充至 20 套模板：新增房东阿姨、部门经理、前台小姐、保安大叔、外卖小哥、便利店店员、护士小姐、出租车司机、老教授、街头混混（普通人脸设计，与主角模板拉开层次）。
- 面部锚点前置+五要素互斥（脸型/眼型/鼻唇/标记物/肤色）解决同脸问题；重写西装总裁/冷峻刑警/痞帅酒吧老板/钢琴艺术家四位男主的面部描述。
- 素材生成切换至新 API 端点。

**内置立绘模板扩充至每包 10 套（图包 v1.3.0）**

- 四包各扩充到 10 套原型角色模板（共 40 角色 400 张），表情差分升级为「表情+配套动作」模式（每格不同手势/头肩姿态）。
- 原型改名：学园少女→黑长直少女、仙门师姐→白发剑仙子、世家公子→玉冠公子、都市男友→西装总裁。

**内置立绘模板与路人剪影（图包 v1.2.0）**

- 四个内置图包各附带 1 套原型角色立绘模板（10 表情差分，2K 表情表切片，chroma-key 抠图为透明 webp）：学园少女（动漫）/ 仙门师姐（水墨）/ 世家公子（3D）/ 都市男友（恋与深空式 3D）。
- 模板不自动入库：立绘上传弹窗新增「内置模板」tab，选模板一键套用到自己的角色名下（URL 引用，可多角色复用）。
- 每包附 3 张路人剪影（路人/路人男/路人女），导入图包自动入库；无立绘角色自动以剪影回退显示（sprite-manager 按声线匹配性别）。
- 生成工具链：tools/gen-sprite-images.js（gpt-5.4-image-2 网格模板法表情表 + gemini 基准图/NPC）、tools/build-sprite-pack.js（抠图/despill/差分对齐/webp/manifest）。

**内置背景图包**

- 资源管理器 → 导入 菜单新增「内置背景图包」：内置日式学园 / 仙侠 / 古装 / 现代都市四套 2K（2560×1440）写实电影感背景，各 16 个场景，共 64 张。
- 图包托管在 GitHub（jsDelivr CDN，gcore 镜像自动回退），导入仅保存链接、不占本地存储；支持缩略图预览、一键导入、幂等重复导入（同名包复用）。
- 场景名为通用中文词（教室 / 竹林 / 宫殿 / 雨夜街头…），与 AI 输出的 `<background scene>` 模糊匹配。
- 素材为 AI 生成（CC0），仓库：bigmalove/galgame-bg-{jp,xianxia,guzhuang,city}@v1.1.0（每包 30 个场景，共 120 张）。
- 新增工具链 `tools/gen-bg-images.js`（批量生图）与 `tools/build-bg-pack.js`（webp 压缩 + manifest 打包）。
- 新增文档 `docs/resources/builtin-bg-packs.md`（含第三方自制图包格式说明）。

## [0.8] - 2026-02-13

### 修复

- 修复 769-1000px（平板/小桌面宽度）下，设置面板/选项面板弹窗可能被遮挡或不显示（恢复为视口尺寸 `100vw/100vh/100dvh`）。
- 修复 `#gal-live2d-settings-modal` 在 769-1000px 下被遮挡的问题（同上）。
- 改善「生图配置」里“背景图来源”单选项文字对比度，避免浅色背景看不清。

## [0.7] - 2026-02-06

### ✨ 新增功能

**galgame通用生成器插件系统**


### 🎨 界面与特效

**数据库界面视觉系统**

- 全新界面设计：采用现代化 UI 风格，301+ 精心设计的 CSS 样式规则。
- 流畅动画效果：全面引入 CSS3 动画，包括淡入淡出、滑动、缩放等交互反馈。
- 自适应布局：基于 Flexbox 和 Grid 的响应式布局，完美适配各种屏幕尺寸。
- 主题变量系统：支持 CSS 变量动态切换，可自定义配色方案和视觉风格。
- 组件化样式：按钮、表单、表格、卡片等常用组件一应俱全，开箱即用。

### ⚡ 性能优化

**核心性能优化**

- 代码体积优化：通过 Tree Shaking 和代码分割，大幅减少加载时间。
- 异步加载机制：关键功能采用 Promise 和异步编程，避免阻塞主线程。
- 缓存策略：实现多级缓存机制，重复操作毫秒级响应。
- 内存管理：优化大数据量处理逻辑，减少内存占用和泄漏风险。

### 🧠 智能体验优化

**智能交互体验**



### 📚 文档




---
