# 状态弹窗

## 用途

状态弹窗让 AI 可以在画面上展示额外的情境信息，比如当前地点和时间。

覆盖层右上角有两个小图标，点击后会弹出对应的状态信息。

## 弹窗标签

AI 在输出中使用以下标签来设置弹窗内容：

| 标签 | 含义 | 默认图标 |
|---|---|---|
| `<弹窗一>...</弹窗一>` | 位置信息 | 📍 定位图标 |
| `<弹窗二>...</弹窗二>` | 时间信息 | 🕐 时钟图标 |

## 自定义图标

你可以在设置中自定义弹窗图标，使用 [Font Awesome](https://fontawesome.com/) 图标类名。
