{
  "name": "galgame-ui-plugin",
  "version": "2.2.5",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "deploy": "node esbuild.config.js && scp dist/数据库界面插件.dist.js yu:~/SillyTavern/public/"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
